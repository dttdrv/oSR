current_phase: phase_4
build_status:
  configured: true
  compiled: partial
  tests_passed: true
  last_checked: 2026-04-29
  last_preset: direct-mingw-manual
  notes:
    - Previous full Ninja build passed at commit e91acfd.
    - Current CMake/Ninja configure/build stalls during compiler ABI/tool execution in this sandbox; changed test sources compile and pass via direct MinGW commands.
    - Visual Studio/MSBuild command-line tools were not available in this sandbox, so the AMD sample solution was not built.
    - Standalone WebGL 3D wind tunnel script passed a Node syntax check.
    - Playwright loaded the standalone 3D wind tunnel over temporary localhost and observed the expected page title.
    - Native Win32 3D wind tunnel with settings panel compiled with direct MinGW and stayed open during launch smoke tests.
    - DX12 wind tunnel proof of life compiled and ran, creating D3D12 resources for color/output/depth/MV/reactive and validating FrameContext with zero errors.
    - H1 buffer-truth path uploads synthetic color/depth/MV/reactive inputs to D3D12 textures, reads them back, hashes canonical rows, and writes a capture pack.
    - H2 DX12 presentation smoke path opens a swapchain window, copies color_output to the backbuffer, presents frames, and supports --headless / --present-frames lab modes.
    - Capture packs now include PPM/PGM debug images, exact raw buffers, and artifacts.json manifests for synthetic frame inputs/output.
    - DX12 wind tunnel supports --mv-mode correct|zero|flip-x|flip-y|half-scale|double-scale|jitter-contaminated for motion-vector truth-table testing.
    - Capture metrics now include CPU temporal diagnostics for MV luma/depth residuals and history-trust confusion rates.
    - DX12 wind tunnel supports --metric-gate / --fail-on-diagnostics and returns exit code 3 for severe temporal diagnostic verdicts.
    - tools/run_dx12_mv_sweep.bat runs all current MV truth-table modes headlessly and fail-fast.
    - DX12 debug upscale backend records and executes a real GPU copy path for native-size color input/output resources.
    - Scaled DX12 debug output now records and executes a compute spatial debug upscale path, then readbacks and hashes color_output_after_dispatch against the CPU nearest reference.
    - The current DX12 compute shader is runtime-compiled from embedded HLSL for prototype speed; production packaging should embed bytecode.
    - DX12 wind tunnel supports --reconstruction spatial-gpu|temporal-cpu.
    - The first temporal-cpu mode blends display-space history conservatively and suppresses history in reactive/high-motion regions.
    - osr_sequence_lab.exe compares spatial versus temporal-cpu over deterministic multi-frame synthetic sequences and writes CSV metrics.
    - osr_dx12_wind_tunnel.exe supports --headless --frames N for deterministic sequence metrics through the main DX12 harness executable.
    - Sequence metric gates now include stability improvement, motion ghost proxy, and reactive trail proxy.
    - CPU temporal resolve now uses motion-compensated display-space history lookup from current-to-previous render-pixel motion vectors.
    - CPU temporal resolve rejects history using luma residuals at the reprojected sample and gates sequence runs on edge preservation.
    - CPU temporal resolve rejects history using depth residuals at the reprojected render-space previous-depth sample.
    - Sequence metrics and DX12 metadata now expose depth_rejected_pct and depth_residual_mean.
    - Default motion rejection is tightened to 2 render pixels after jitter-aware history accumulation exposed excessive low-resolution motion trust.
    - CPU temporal resolve uses bilinear sampling for reprojected display history and previous-depth residual validation.
    - Temporal diagnostics now use the same bilinear subpixel luma/depth reprojection model as the CPU temporal resolve.
    - Sequence lab and DX12 temporal-cpu mode now use a bilinear spatial base instead of nearest-neighbor color.
    - DX12 temporal-cpu capture packs now include history_weight, color_residual, and depth_residual debug maps.
    - Native 3D wind-tunnel executable exposes color, luma, depth, edge-energy, history-weight proxy, and rejection-risk proxy views.
    - DX12 compute spatial debug upscale now uses bilinear sampling and verifies readback with exact hashes plus max/mean byte deltas.
    - DX12 wind tunnel supports --reconstruction temporal-gpu for a single-frame GPU temporal resolve proof of life against the CPU oracle.
    - DX12 wind tunnel supports --headless --reconstruction temporal-gpu --frames N with persistent GPU output history copied forward each frame.
    - CPU and DX12 temporal resolve paths apply confidence-gated detail recovery and report sharpening_amount_mean.
    - Sequence and DX12 metric gates now include thin_feature_contrast to protect high-frequency edge/text-like regions from blur or excessive ringing.
    - Synthetic and native 3D wind tunnels include readable in-scene block text targets.
    - Sequence and DX12 metric gates now include text_readability_contrast to prevent temporal wins that smear readable glyphs.
    - Synthetic and native 3D wind tunnels include specular glint and transparent-pane material stress targets.
    - Sequence and DX12 metric gates now include specular_history_leak and transparent_history_leak from temporal debug history-weight maps.
    - Capture-pack metrics.csv now includes thin/text/material ROI metric columns and temporal captures populate material leak values.
    - Temporal-gpu shader writes history_weight, color_residual, and depth_residual R32F debug UAVs.
    - Single-frame temporal-gpu capture packs dump shader-produced temporal debug maps as PGM artifacts.
    - Single-frame temporal-gpu runs compare shader debug maps against CPU oracle maps and fail on broad drift.
    - Temporal-gpu sequence runs support --capture-frame / --capture-frame-id to dump one selected frame's GPU debug maps into a normal capture pack.
    - Capture-pack metrics include per-map CPU-vs-GPU debug parity columns for history weight, color residual, and depth residual.
    - Temporal diagnostics use a 0.90 reactive trust penalty to align with the resolve path's reactive-history suppression policy.
    - Temporal resolve now uses a 0.98 clean-history ceiling with stronger motion rejection to recover stable jittered detail without trusting moving edges.
    - DX12 temporal shader now quantizes the temporal blend before detail recovery to match the CPU oracle ordering.
    - CPU sequence metrics and DX12 temporal shader sampling now compensate current-frame jitter before reconstructing display-space color.
    - Temporal-gpu sequence parity uses read-back GPU output as the next CPU oracle history seed so the gate measures per-dispatch shader drift instead of compounding CPU/GPU R8 feedback differences.
    - Capture packs now include signed motion-vector X/Y PGM views plus raw R32F temporal debug maps for history weight, color residual, and depth residual.
    - tools/run_capture_analyzer.bat builds an offline capture analyzer that summarizes raw temporal maps and motion vectors without rerunning DX12.
    - DX12 wind tunnel temporal CPU/GPU paths expose CLI tuning flags for history weight, reactive penalty, motion/color/depth rejection, and confidence-gated sharpening.
    - tools/run_dx12_mv_sweep.bat now asserts that correct motion vectors pass metric gates and corrupted MV modes fail them.
    - CPU and DX12 temporal resolve paths clamp reprojected history color to a current-frame cross-neighborhood before residual testing and blending.
    - Offline capture analysis reports motion/static, text, specular, transparent, and reactive ROI history trust using shared synthetic ROI predicates.
    - tools/run_capture_analyzer.bat supports --gate for saved-frame ROI trust regression checks.
    - DX12 temporal selected-frame captures run the ROI capture-analysis gate in-process when --metric-gate is enabled.
    - DX12 temporal sequence parity reports worst byte-diff frame, pixel, channel, and synthetic ROI class.
    - Capture analysis gates use named threshold settings and can load overrides from simple key/value threshold config files.
    - profiles/capture_gate.cfg contains the default synthetic capture-analysis gate thresholds used for local sweeps.
    - DX12 selected-frame capture gates can load the same capture threshold config through --capture-gate-thresholds.
    - Selected temporal-GPU capture packs snapshot the active capture gate threshold file as capture_gate_thresholds.cfg and record threshold provenance in session.json.
    - Selected temporal-GPU capture packs write capture_analysis.json with global, motion/static, ROI, gate, and threshold data.
    - tools/run_capture_compare.bat ranks capture sessions from capture_analysis.json with a hard-gated higher-is-better ROI/trust score.
    - tools/run_temporal_tuning_sweep.bat runs deterministic named temporal-GPU tuning captures and ranks them; current local winner is sweep_conservative.
    - The default CPU and DX12 temporal resolve settings now use the sweep_conservative values: history 0.92, reactive penalty 0.95, motion 1.25, color 0.12, depth 0.025, clip 0.02.
    - tools/run_temporal_frame_sweep.bat ranks default temporal-GPU captures for frames 10/12/14; selected frame captures at 16/18 currently fail the debug-map parity gate.
    - Selected temporal-GPU captures write debug_parity.json; frame 16's current failure is a localized history-weight drift at display pixel 928,342.
    - Quality/Balanced/Performance render-scale presets are now exact 0.66/0.58/0.50 across core, DX12, native 3D, and WebGL harness defaults.
    - tools/run_xess_proxy_smoke.bat builds an opt-in diagnostic libxess.dll proxy, verifies it loads, and verifies safe missing-libxess_real.dll logging.
    - Installed Steam No Man's Sky imports libxess.dll and references xessGetInputResolution, xessVKCreateContext, xessVKInit, xessVKExecute, and xessGetProperties; current settings select XESS Balanced.
    - tools/install_xess_proxy_nms.bat and tools/restore_xess_proxy_nms.bat provide controlled install/restore for the No Man's Sky XeSS proxy proof.
    - The real Steam No Man's Sky Binaries folder now has oSR's diagnostic libxess.dll installed and the original Intel runtime preserved as libxess_real.dll.
    - Real No Man's Sky launch proof reached the XeSS Vulkan dispatch boundary: proxy log contains real runtime load, input-resolution/property queries, xessVKInit, and repeated xessVKExecute calls.
    - The installed No Man's Sky proxy build now throttles execute logging after the first 16 dispatches and logs every 120th dispatch afterward.
    - CPU oracle and DX12 temporal shader now use matching YCoCg history clipping instead of independent RGB channel clipping.
    - Trust Field v2 feature-lock groundwork exists as a standalone tested reconstruction module.
    - CPU temporal resolve emits feature_lock_strength diagnostics, and feature locks are now allowed to conservatively influence detail recovery.
    - Wind-tunnel capture dumps now export feature_lock_strength PGM/raw artifacts in artifacts.json when temporal debug maps are available.
    - Capture analysis now reports optional global and ROI feature-lock metrics.
    - CPU and DX12 temporal resolve now use feature-lock evidence to conservatively boost detail recovery on stable high-trust edges.
    - DX12 temporal resolve now emits feature_lock_strength as a GPU debug UAV, and selected captures include CPU-vs-GPU feature-lock parity.
    - Capture dumps now include an optional spatial_baseline artifact, and capture analysis gates a baseline-aware locked_detail score.
    - Capture analysis now separates all/static/moving text ROI metrics; locked_detail scoring prefers static text when available.
    - Default temporal sharpening is tuned to 0.40 after a split-text DX12 temporal sweep beat the previous 0.28 default without adding moving/reactive feature locks.
    - Default history_clip_margin is tuned to 0.015 after a guarded DX12 sweep beat the previous 0.02 aggregate capture score without lowering text trusted history.
    - Capture compare now reports text_contrast_ratio and bad_lock_signal directly, and ranking includes a small bad-lock penalty.
external_dependencies:
  fidelityfx_sdk:
    path: external/FidelityFX-SDK
    git_commit: e236f23
    version_observed: AMD FSR SDK 2.2.0 Redstone, FSR upscaler 3.1.5
    status: installed_locally_not_vendored
known_working_assumptions:
  - Windows 11 primary target.
  - Primary hardware target is AMD Radeon 760M / Ryzen 5 7640HS class laptop.
  - Output target is 1920x1200.
  - Initial bridge is FSR3.1 / FidelityFX API style DX12 input.
  - Reference projects are clean-room references only unless license review changes that.
  - SDK/sample apps are the first validation targets.
  - oSR's research differentiator is trust-field temporal reconstruction with explainable confidence-driven history selection.
  - Manual quality control uses DLSS-style presets plus a custom render-scale slider clamped to one-third through native resolution.
unknowns:
  - Final exported DLL names for bridge packaging beyond the XeSS diagnostic proxy.
  - Exact XeSS SDK header/ABI version and execute-parameter struct layout used by the installed No Man's Sky build.
  - Motion-vector convention, exposure behavior, and responsive-mask availability for the first real-game target.
target_games_for_initial_testing:
  samples:
    - AMD FidelityFX SDK FSR3 upscaler sample
    - Local synthetic DX12 harness
    - DX12 wind tunnel proof of life with synthetic SR input resources
    - Native Win32 3D wind tunnel executable for movement/settings/readable diagnostics
    - Standalone WebGL 3D wind tunnel for visual edge/jitter/render-scale inspection
  later_offline_games:
    - No Man's Sky
    - The Outer Worlds: Spacer's Choice Edition
    - RoboCop: Rogue City
next_actions:
  - Replace runtime HLSL compilation with embedded shader bytecode.
  - Add GPU timestamp queries for upload, reconstruct, readback, and present.
  - Use tools/run_3d_wind_tunnel.bat as the primary manual visual harness.
  - Use tools/run_dx12_wind_tunnel.bat to verify real D3D12 resource-backed FrameContext export.
  - Use tools/run_3d_scene.bat for visual manual testing of render scale, jitter, thin-geometry aliasing, and debug view controls.
  - Build AMD FidelityFX_FSR_2022.sln once Visual Studio/MSBuild is available.
  - Wire FFX-style sample dispatch into FrameContext logging.
  - Run first sample proof of life and append results to LOG.md.
  - Use --mv-mode sweeps to tune temporal trust thresholds and verify bad history is rejected before adding GPU temporal accumulation.
  - Promote the temporal diagnostic verdict thresholds from initial coarse gates to scene-specific gates after multi-frame scenarios exist.
  - Add headless multi-frame sweeps that compare correct vs corrupted motion-vector modes and fail on trust-regression thresholds.
  - Refactor DX12 wind tunnel --frames N from CPU sequence metric mode into a true per-frame DX12 capture loop with capture-pack rows.
  - Add shimmer/ghost/text/edge metrics over multi-frame sequences, not only one-frame diagnostics.
  - Add confidence-gated edge/text ROI metrics so readability and thin-geometry stability become first-class gates.
  - Replace manual harness history-weight/rejection-risk proxy views with real temporal resolve debug maps once the harness owns frame history.
  - Add location-aware reporting for the worst temporal-gpu sequence parity pixel so jittered edge outliers can be classified by ROI.
  - Investigate selected temporal-GPU debug-map parity failures at frames 16 and 18 before expanding the frame sweep further.
  - Add temporal shader debug UAVs for history_weight, color_residual, and depth_residual.
  - Add ROI-labeled per-region capture rows for text, specular glints, transparent panes, reactive particles, and corrupted-MV stress regions.
  - Add automated gates for selected sequence debug-map parity metrics after thresholds are calibrated across more frame IDs.
  - Add CPU-vs-HLSL numeric parity tests once shader compilation is wired.
  - Use tile classification to gate the first motion-vector residual search prototype.
  - Port bounded residual search to HLSL behind MotionRisk/ShimmerRisk tiles.
  - Tune feature-lock thresholds using locked_detail_score, text_contrast_ratio, and bad_lock_signal instead of the older ROI/history score alone.
  - Add a max_bad_lock_signal capture gate threshold so transparent/specular lock leakage can fail captures directly, not only reduce ranking.
  - Run a guarded feature-lock threshold sweep after max_bad_lock_signal is enforced.
  - Add a saved high-resolution reference or supersampled oracle artifact so future detail scoring can move beyond spatial-baseline comparisons.
  - Fold confidence-gated sharpening into the first real reconstruction pass.
  - Surface quality_mode/custom_render_scale in the future DX12 harness UI as a segmented preset control plus slider.
  - Add throttled XeSS execute-parameter metadata decoding for No Man's Sky once the exact SDK header version is identified, without dereferencing unknown resources unsafely.
